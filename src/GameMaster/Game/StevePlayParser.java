package GameMaster.Game;

import Play.BuildPhase.BuildPhase;
import Play.TilePlacementPhase.TilePlacementPhase;

public class StevePlayParser {
    public static String parse(BuildPhase buildPhase, TilePlacementPhase tilePlacementPhase) {
        //TODO:
        // return message with play info in format
        // PLACE <tile> AT <x> <y> <z> <orientation> BUILD TIGER PLAYGROUND AT <x> <y> <z>
        // according to the rules. The mapping needs to be done, and Z coordinate
        // needs to be generated
        return null;
    }
}
