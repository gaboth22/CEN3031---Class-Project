package Tile.Tile;


import Location.Location;
import Terrain.Terrain.Terrain;
import java.util.List;

public class FirstTileImpl extends TileImpl {
    public FirstTileImpl(List<Terrain> terrainList, List<Location> locationList){
        super(terrainList, locationList);
    }
}
