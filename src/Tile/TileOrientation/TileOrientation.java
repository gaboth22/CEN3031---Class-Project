package Tile.TileOrientation;

/*
 * The convention is that the orientation is the orientation of the volcano
 * from a point at the center of the tile
 */
public enum TileOrientation {
    UP,
    DOWN,
    LEFT,
    RIGHT
}
